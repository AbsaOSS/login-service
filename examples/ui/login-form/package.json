{
  "name": "login-service-ui-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Login Service MS Entra login form example",
  "scripts": {
    "build": "esbuild src/app.js --bundle --format=iife --minify --outfile=dist/bundle.js",
    "prepare": "npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "@azure/msal-browser": "^2.38.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
